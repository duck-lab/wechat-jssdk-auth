import { Test, TestingModule } from '@nestjs/testing';
import { JSSDKController } from './controller';
import { JSSDKService } from './service';

describe('AppController', () => {
  // let app: TestingModule;
  // beforeAll(async () => {
  //   app = await Test.createTestingModule({
  //     controllers: [JSSDKController],
  //     providers: [JSSDKService],
  //   }).compile();
  // });
  // describe('root', () => {
  //   it('should return "Hello World!"', () => {
  //     const sdkController = app.get<JSSDKController>(JSSDKController);
  //     expect(sdkController.root()).toBe('Hello World!');
  //   });
  // });
});
